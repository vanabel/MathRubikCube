# 核心功能无需外部依赖

# 可视化依赖（推荐）
matplotlib>=3.3.0      # 二维可视化（强烈推荐）

# Web应用依赖（可选）
streamlit>=1.28.0      # Web交互界面

# 其他可选依赖（用于高级功能）
# numpy>=1.20.0          # 高效的数组运算
# sympy>=1.9             # 符号计算和群论分析
# vpython>=7.6.0         # 三维可视化
# pytest>=6.0.0          # 测试框架

